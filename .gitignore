# Build artifacts
bin/
obj/
*.nupkg
*.snupkg
artifacts/

# SQLite runtime database (generated by the indexer, never committed)
*.db
*.db-shm
*.db-wal

# Retrieval-eval run reports (only eval/baseline.json is committed)
eval/results/

# Local/dev-only MCP configs (machine-specific absolute paths). Anchored to the
# repo root so it doesn't also match plugins/*/.mcp.json, which is committed.
/mcp-config.json
/.mcp.json

# IDE
.vs/
.vscode/
.idea/
*.user

# OS
.DS_Store
Thumbs.db

# Jekyll
docs/_site/
docs/.jekyll-cache/
docs/.bundle/
docs/vendor/
